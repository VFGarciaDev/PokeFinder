import { StyledHeader } from "./styledHeader"

export const Header = () => {
    return (
        <StyledHeader>
            <img src="#" alt="Logo" />
            <h1>Poke<span className="red">Finder</span></h1>
            <p>Tema</p>
        </StyledHeader>
    )
}